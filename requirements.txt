# Core web crawling
requests==2.31.0
beautifulsoup4==4.12.2
lxml  # Let pip find the best compatible version
html2text  # For markdown conversion

# Data processing and validation
pandas  # Let pip find the best compatible version
pydantic  # For data validation and models (flexible version)

# URL and content utilities
validators  # For URL validation
tldextract  # For domain parsing

# CLI and configuration  
click # For better CLI interface
python-dotenv  # For environment variables

# Development tools
ruff==0.1.6  # For linting
mypy==1.7.0  # For type checking
pytest==7.4.3  # For testing
pytest-mock==3.12.0  # For mocking in tests

# Phase 2+ dependencies (optional for now)
# crawl4ai==0.7.4  # Advanced content extraction
# markdownify==0.11.6  # Alternative markdown converter
# scrapy==2.11.0  # Alternative crawling framework
# playwright==1.40.0  # Browser automation
# selenium==4.15.2  # Browser automation